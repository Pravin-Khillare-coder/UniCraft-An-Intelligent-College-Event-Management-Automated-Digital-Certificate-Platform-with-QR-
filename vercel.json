{
  "installCommand": "npm install --legacy-peer-deps --no-audit --no-fund && cd backend && npm install --no-audit --no-fund && cd ../frontend && npm install --legacy-peer-deps --no-audit --no-fund",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
