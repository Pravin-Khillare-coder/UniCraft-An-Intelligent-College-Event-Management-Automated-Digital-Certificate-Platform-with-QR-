services:
  - type: web
    name: unicraft-event-backend
    env: node
    buildCommand: npm install --prefix backend
    startCommand: node backend/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: USE_MOCK_DB
        value: "true"
